// const JWT = localStorage.getItem('jwt') || location.replace('/');

// let jwtParce = JSON.parse(atob(JWT.split('.')[1]));

// if(jwtParce.role[0].authority === "ROLE_USER"){
//   const rolRestriction = document.querySelectorAll(".rol");
//   rolRestriction.forEach(elemento =>{
//     elemento.classList.add("disabled")
//   })
// }